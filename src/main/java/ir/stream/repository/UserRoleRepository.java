package ir.stream.repository;

import ir.stream.core.repository.AbstractCrudRepository;
import ir.stream.entity.UserRole;

public interface UserRoleRepository extends AbstractCrudRepository<UserRole, Long> {
}
